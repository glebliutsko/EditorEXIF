<Window x:Class="PhotoDateEditor.Windows.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpfTool="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
        xmlns:local="clr-namespace:PhotoDateEditor.Windows" mc:Ignorable="d" Title="Редактирование даты фотографии"
        Height="300" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox x:Name="PhotosListBox" SelectionMode="Extended" Background="#FFF7F7F7" Grid.Column="0" AllowDrop="True"
                Drop="PhotosListBox_Drop">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image MaxHeight="100" MaxWidth="100" Source="{Binding PathToFile}" Stretch="UniformToFill" />
                        <StackPanel>
                            <TextBlock Text="{Binding FileName}" FontSize="16" FontWeight="Bold" />
                            <TextBlock Text="{Binding CreateDateTime}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <DockPanel Grid.Column="1">
            <Button Margin="10 0 0 0" Padding="10 2" DockPanel.Dock="Right" Content="Сохранить изменения" />
            <wpfTool:DateTimePicker Padding="0 2" VerticalContentAlignment="Center" HorizontalContentAlignment="Left"
                    DockPanel.Dock="Right" Format="Custom" FormatString="dd.MM.yyyy hh:mm:ss" />
        </DockPanel>
    </Grid>
</Window>
